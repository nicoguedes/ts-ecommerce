export default class Product{
    price: number;
    description: String;

    constructor(price: number, description: String){
        this.price = price;
        this.description = description;
    }
}